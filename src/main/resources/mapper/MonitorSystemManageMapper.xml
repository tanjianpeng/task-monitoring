<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ganzhou.monitoring.mapper.MonitorSystemManageMapper">

    <!--
        查询全部系统主数据。
        用途：用于系统管理列表和本地大屏系统列表展示。
    -->
    <select id="selectAll" resultType="MonitorSystem">
        select system_code,
               system_name,
               owner_name,
               supervisor_name,
               is_flag,
               remark,
               created_time,
               updated_time
        from monitor_system
        order by system_code asc
    </select>

    <!--
        按系统编码查询单个系统信息。
        用途：用于系统详情查看和编辑前回显。
        入参：
        1. systemCode：系统编码，唯一标识一条系统记录。
    -->
    <select id="selectByCode" resultType="MonitorSystem">
        select system_code,
               system_name,
               owner_name,
               supervisor_name,
               is_flag,
               remark,
               created_time,
               updated_time
        from monitor_system
        where system_code = #{systemCode}
        limit 1
    </select>

    <!--
        新增系统主数据。
        用途：保存一条新的监控系统配置。
        入参：
        1. systemCode：系统编码。
        2. systemName：系统名称。
        3. ownerName：系统负责人。
        4. supervisorName：监督人。
        5. isFlag：是否启用状态。
        6. remark：备注。
    -->
    <insert id="insert" parameterType="MonitorSystem">
        insert into monitor_system (
            system_code, system_name, owner_name, supervisor_name,
            is_flag, remark
        ) values (
            #{systemCode}, #{systemName}, #{ownerName}, #{supervisorName},
            #{isFlag}, #{remark}
        )
    </insert>

    <!--
        按系统编码更新系统主数据。
        用途：修改系统名称、负责人、监督人、状态等信息。
        入参：
        1. systemCode：系统编码，表示更新哪条记录。
        2. 其余字段：要更新成的新值。
    -->
    <update id="updateByCode" parameterType="MonitorSystem">
        update monitor_system
        set system_name = #{systemName},
            owner_name = #{ownerName},
            supervisor_name = #{supervisorName},
            is_flag = #{isFlag},
            remark = #{remark},
            updated_time = now()
        where system_code = #{systemCode}
    </update>

    <!--
        按系统编码删除系统主数据。
        用途：删除不再使用的系统配置。
        入参：
        1. systemCode：系统编码，表示要删除的系统。
    -->
    <delete id="deleteByCode">
        delete from monitor_system
        where system_code = #{systemCode}
    </delete>
</mapper>
